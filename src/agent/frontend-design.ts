// 프론트엔드 디자인 지침 (bcave-agent 이식판).
// Anthropic 의 frontend-design 스킬 원칙을 bcave-agent(OpenAI·gpt-5.4-mini·저토큰)에 맞게
// 압축·번역해 정리한 것 (원문 그대로가 아닌 원칙 요약). UI/시각 결과물 생성 시 프롬프트에 주입한다.

export const FRONTEND_DESIGN = `[프론트엔드 디자인 원칙 — 작은 스튜디오의 디자인 리드처럼, "템플릿 같지 않게"]

먼저 주제를 잡아라: 무엇을(제품/주제)·누구를 위해·이 화면의 단 하나의 역할을 정하고 그에 맞춰 디자인해라. 정보가 부족하면 합리적으로 스스로 정하고 밝혀라.

의도적으로 선택하라 (기본값 회피):
- 색: 주제에 맞는 팔레트를 4~6개 hex 로 정해라(이름 붙여). 주색은 절제해 강조에만, 상태(증가↑/감소↓)에도 활용. 무의미한 그라데이션·원색 남발 금지.
- 타이포: 글꼴은 **Pretendard 하나로 통일**하고, 제목/본문의 인상은 서체 교체가 아니라 크기·굵기(Weight)·자간·행간 위계로 만들어라 — 타이포 자체가 인상적인 요소가 되게.
- 구조: 번호(01/02/03)·구분선·라벨은 실제 의미(순서·과정)가 있을 때만. 장식용 금지.
- 시그니처: 이 화면을 기억하게 할 "단 하나의" 특징 요소를 정하고, 나머지는 조용하고 정돈되게.
- 모션: 필요한 곳에만 절제해서. 과한 애니메이션은 오히려 'AI가 만든 티'가 난다.

흔한 AI 기본값 3가지는 피하라 (요청에 명시되지 않는 한):
① 크림색 배경 + 고대비 세리프 + 테라코타 강조  ② 거의 검정 배경 + 형광(연두/주홍) 강조 1색  ③ 신문 레이아웃(가는 선·모서리 0·빽빽한 칼럼).

먼저 색/타이포/레이아웃/시그니처를 짧게 계획하고, 그게 "아무 프로젝트에나 나올 기본값"처럼 보이면 고쳐라(무엇을 왜 바꿨는지 한 줄). 확인 후 그 계획대로 코드를 작성해라. CSS 는 셀렉터 우선순위가 서로 상쇄되지 않게(특히 섹션 padding/margin) 주의.

품질 기준(항상 지킬 것): 모바일까지 반응형, 키보드 포커스 표시, prefers-reduced-motion 존중, 충분한 명도 대비(색만으로 정보 전달 금지).

카피(글도 디자인이다): 사용자 입장의 쉬운 말·능동태, 일관된 용어(버튼 'Publish' → 토스트 'Published'). 빈 화면·오류는 "무엇이 왜, 어떻게 하면 되는지" 안내로. 각 요소는 한 가지 역할만.`;
